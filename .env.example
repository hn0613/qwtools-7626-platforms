# Upstash Redis (required for tenant storage)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Root domain for the platform (e.g. example.com)
NEXT_PUBLIC_ROOT_DOMAIN=localhost:3000

# Admin access password (required to protect the admin dashboard)
ADMIN_PASSWORD=
